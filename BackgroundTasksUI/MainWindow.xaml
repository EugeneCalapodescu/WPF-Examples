<Window x:Class="WPF_examples.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_examples"
        mc:Ignorable="d"
        Title="Background task" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="5" >
            <TextBlock FontSize="20" FontWeight="Bold"> Background Task Updating the UI</TextBlock>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5" >
            <Button x:Name="b_start" Width="100" Click="b_start_Click">Start</Button>
            <Button x:Name="b_stop" Width="100" Margin="10 0" IsEnabled="False" Click="b_stop_Click">Stop</Button>
            <TextBox x:Name="tb_counter" Width="100">1</TextBox>
        </StackPanel>

        
        
    </Grid>
    
    
</Window>
